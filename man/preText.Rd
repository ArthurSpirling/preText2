% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/preText.R
\name{preText}
\alias{preText}
\title{preText}
\usage{
preText(
  preprocessed_documents,
  dataset_name = "Documents",
  distance_method = "cosine",
  num_comparisons = 50,
  parallel = FALSE,
  cores = 1,
  verbose = TRUE
)
}
\arguments{
\item{preprocessed_documents}{A list object generated by the
\code{factorial_preprocessing()} function.}

\item{dataset_name}{A string indicating the name to be associated with the
results. Defaults to "Documents".}

\item{distance_method}{The method that should be used for calculating
document distances. Defaults to "cosine".}

\item{num_comparisons}{The number of ranks to use in calculating average
difference. Defaults to 50.}

\item{parallel}{Logical indicating whether processing should be
performed in parallel. Defaults to FALSE.}

\item{cores}{Defaults to 1.}

\item{verbose}{Logical indicating whether to print progress. Defaults to TRUE.}
}
\value{
A result list object with elements:
\itemize{
  \item \code{preText_scores}: data.frame of preText scores
  \item \code{ranked_preText_scores}: sorted preText scores
  \item \code{choices}: data.frame of preprocessing choices
  \item \code{regression_results}: regression results data.frame
}
}
\description{
Calculates preText scores for each preprocessing specification.
This is the main wrapper function that runs the full preText procedure.
}
\examples{
\dontrun{
library(preText2)
corp <- quanteda::data_corpus_inaugural
documents <- as.character(head(corp, 10))
preprocessed_documents <- factorial_preprocessing(
    documents,
    use_ngrams = TRUE,
    infrequent_term_threshold = 0.2,
    verbose = TRUE)
preText_results <- preText(
    preprocessed_documents,
    dataset_name = "Inaugural Speeches",
    distance_method = "cosine",
    num_comparisons = 20,
    verbose = TRUE)
}
}
